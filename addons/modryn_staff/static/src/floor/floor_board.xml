<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- A customer card's people: the primary slot + helper chips. Chips are
         draggable (managers only — the hook is disabled otherwise); the primary
         slot is its own drop zone so dropping there swaps rather than adds. -->
    <t t-name="modryn_staff.CardPeople">
        <div class="modryn_card_people">
            <div class="modryn_primary_slot"
                 t-att-data-drop-target="kind" t-att-data-drop-id="card.id"
                 data-drop-primary="1">
                <span t-if="card.employee_id" class="modryn_chip is_primary"
                      t-att-data-employee="card.employee_id">
                    <t t-esc="card.employee_name"/>
                </span>
                <span t-else="" class="modryn_slot_empty">Primary</span>
            </div>
            <t t-foreach="card.helpers" t-as="h" t-key="h.id">
                <span class="modryn_chip is_helper" t-att-data-employee="h.id">
                    <t t-esc="h.name"/>
                </span>
            </t>
        </div>
    </t>

    <t t-name="modryn_staff.FloorBoard">
        <div class="modryn_floor" t-ref="root"
             t-att-class="state.dragging ? 'is_dragging' : ''">

            <div t-if="state.error" class="modryn_field_error mb-3" role="alert">
                <t t-esc="state.error"/>
            </div>

            <!-- Not a refusal: the assignment went through. role="status", not
                 "alert", for that reason. Transient like state.error above —
                 every apply() clears it and the bus refreshes often — which is
                 the intent: a nudge at the moment of the drop, not a notice that
                 sits on the board all day. Already translated by the server. -->
            <div t-if="state.warning" class="modryn_field_warning mb-3" role="status">
                <t t-esc="state.warning"/>
            </div>

            <!-- My own call stays here rather than in the overlay: I raised it,
                 I need to see whether anyone picked it up — not be blocked by it. -->
            <div t-foreach="myOwnSos" t-as="c" t-key="c.id" class="modryn_sos_mine mb-3">
                <span>
                    <t t-if="c.state === 'acked'">Someone is on the way.</t>
                    <t t-else="">Waiting for someone to answer…</t>
                    <t t-if="c.escalated"> Passed to the managers.</t>
                </span>
                <button class="btn btn-sm btn-outline-dark"
                        t-on-click="() => this.resolveSos(c.id)">All good now</button>
            </div>

            <div class="modryn_floor_layout">
                <div class="modryn_floor_main">

                    <!-- ====== arrivals: asked to be seen, not yet in line ======
                         The gate the customer never sees. Her page says "we'll be
                         with you soon" from the moment she scans, whichever way
                         this goes. -->
                    <section t-if="state.pending.length" class="modryn_panel modryn_pending">
                        <header class="modryn_panel_head">
                            <h2>Just arrived</h2>
                            <span class="modryn_count"><t t-esc="state.pending.length"/></span>
                        </header>
                        <table class="table modryn_table align-middle">
                            <tbody>
                                <tr t-foreach="state.pending" t-as="p" t-key="p.id">
                                    <td>
                                        <div class="modryn_strong"><t t-esc="p.name"/></div>
                                        <div class="modryn_sub" dir="ltr"><t t-esc="p.phone"/></div>
                                    </td>
                                    <td>
                                        <span class="modryn_badge is_muted">
                                            <t t-esc="clientTypeLabel(p)"/>
                                        </span>
                                    </td>
                                    <td t-if="state.canAssign" class="text-end">
                                        <button class="btn btn-sm btn-primary me-2"
                                                t-on-click="() => this.acceptPending(p.id)">
                                            Add to the line
                                        </button>
                                        <button class="btn btn-sm btn-outline-dark"
                                                t-on-click="() => this.redirectPending(p.id)">
                                            Invite to book
                                        </button>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </section>

                    <!-- ============ walk-in queue ============ -->
                    <!-- Named, because three panels on this board draw the same
                         customer card and nothing could tell them apart. "Is she
                         in the queue as well as with somebody" is a real
                         question about this panel and an entirely wrong one
                         about the appointments below, which are a schedule. -->
                    <section class="modryn_panel modryn_queue_panel">
                        <header class="modryn_panel_head">
                            <h2>Walk-in queue</h2>
                            <span class="modryn_count"><t t-esc="waitingCount"/> waiting</span>
                        </header>

                        <!-- waitingQueue, not state.queue: a customer a stylist
                             has taken is on the panel below, and printing her
                             here as well showed one bride twice and counted her
                             among the people waiting for somebody. -->
                        <p t-if="!waitingQueue.length" class="modryn_empty">No one is waiting.</p>

                        <div class="modryn_cards">
                            <div t-foreach="waitingQueue" t-as="entry" t-key="entry.id"
                                 class="modryn_customer_card"
                                 data-drop-target="queue" t-att-data-drop-id="entry.id">
                                <div class="modryn_card_top">
                                    <span class="modryn_pos"><t t-esc="entry.position"/></span>
                                    <div>
                                        <div class="modryn_strong"><t t-esc="entry.name"/></div>
                                        <div class="modryn_sub" dir="ltr"><t t-esc="entry.phone"/></div>
                                    </div>
                                    <span class="modryn_badge is_muted ms-auto">
                                        <t t-esc="clientTypeLabel(entry)"/>
                                    </span>
                                    <span t-if="entry.state === 'waiting'" class="modryn_badge is_wait">Waiting</span>
                                    <span t-else="" class="modryn_badge is_busy">Being served</span>
                                </div>

                                <t t-call="modryn_staff.CardPeople">
                                    <t t-set="card" t-value="entry"/>
                                    <t t-set="kind" t-value="'queue'"/>
                                </t>

                                <div class="modryn_card_actions">
                                    <select class="form-select form-select-sm w-auto"
                                            t-att-value="entry.room_id or ''"
                                            t-on-change="(ev) => this.setRoom('queue', entry.id, ev)">
                                        <option value="">— No room —</option>
                                        <t t-foreach="state.rooms" t-as="r" t-key="r.id">
                                            <option t-att-value="r.id"
                                                    t-att-selected="r.id === entry.room_id"><t t-esc="r.name"/></option>
                                        </t>
                                    </select>
                                    <button t-if="entry.phone" class="btn btn-sm btn-outline-dark"
                                            t-on-click="() => this.openCustomer(entry.name, entry.phone)">
                                        Profile
                                    </button>
                                    <button class="btn btn-sm btn-outline-danger"
                                            t-on-click="() => this.openSos('queue', entry.id)">
                                        Call for help
                                    </button>
                                </div>

                                <!-- What a saleswoman does with the customer in
                                     front of her. Gated on canTake, NOT on
                                     canAssign: that one boolean also switches on
                                     Done and Invite-to-book, whose routes still
                                     refuse her - she would get buttons that
                                     error. -->
                                <div t-if="state.canTake" class="modryn_card_actions">
                                    <button t-if="!this.iAmOn(entry)"
                                            class="btn btn-sm btn-primary modryn_take_btn"
                                            t-on-click="() => this.take('queue', entry.id)">
                                        I'll take her
                                    </button>
                                    <button t-if="this.iAmOn(entry)"
                                            class="btn btn-sm btn-outline-dark modryn_release_btn"
                                            t-on-click="() => this.release('queue', entry.id)">
                                        Back to the line
                                    </button>
                                    <select class="form-select form-select-sm w-auto"
                                            t-on-change="(ev) => this.setClientType(entry.id, ev)">
                                        <option value="bride"
                                                t-att-selected="entry.client_type === 'bride'">Bride</option>
                                        <option value="evening"
                                                t-att-selected="entry.client_type === 'evening'">Regular customer</option>
                                    </select>
                                </div>
                                <!-- The note. Saved on blur rather than on every
                                     keystroke: each save pushes to every open
                                     board in the shop, and a push per character
                                     would make the other terminals unusable. -->
                                <div t-if="state.canTake" class="modryn_card_note">
                                    <input type="text" class="form-control form-control-sm"
                                           placeholder="Note for the team"
                                           t-att-value="entry.staff_note or ''"
                                           t-on-change="(ev) => this.setNote(entry.id, ev)"/>
                                </div>
                                <div t-if="state.canAssign" class="modryn_card_actions">
                                    <!-- Click fallback: dragging is faster, but a
                                         keyboard or a stubborn tablet still works. -->
                                    <select class="form-select form-select-sm w-auto"
                                            t-on-change="(ev) => this.assignFromSelect('queue', entry.id, ev)">
                                        <option value="">— Assign —</option>
                                        <t t-foreach="state.staff" t-as="s" t-key="s.id">
                                            <option t-att-value="s.id"><t t-esc="s.name"/></option>
                                        </t>
                                    </select>
                                    <button class="btn btn-sm btn-primary"
                                            t-on-click="() => this.finish(entry.id)">Done</button>
                                    <!-- "Invite to book" used to live only in the arrivals
                                         panel, which a verified check-in now skips entirely —
                                         dropping the acceptance gate would otherwise have
                                         taken this capability with it, silently.
                                         modryn_redirect already accepts 'waiting', so only
                                         the button had to move. 'called' is excluded: she is
                                         already with a stylist and the model would no-op. -->
                                    <button t-if="entry.state === 'waiting'"
                                            class="btn btn-sm btn-outline-dark"
                                            t-on-click="() => this.redirectPending(entry.id)">
                                        Invite to book
                                    </button>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- ============ with the team right now ============ -->
                    <!-- Placed between the line and the day's appointments,
                         because that is where it sits in time: she has come off
                         the queue and has not yet become a finished visit. -->
                    <section t-if="withTeam.length" class="modryn_panel modryn_with_team">
                        <header class="modryn_panel_head">
                            <h2>With the team right now</h2>
                            <!-- People, not groups. The groups are on the
                                 screen to be counted; how many customers are
                                 being served is the number that is not. -->
                            <span class="modryn_count"><t t-esc="withTeamCount"/></span>
                        </header>

                        <div class="modryn_cards">
                            <div t-foreach="withTeam" t-as="g" t-key="g.id"
                                 class="modryn_team_group">
                                <div class="modryn_strong mb-2"><t t-esc="g.name"/></div>
                                <!-- Which kind of visit this row is, said in the
                                     markup rather than inferred from which
                                     buttons happen to be on it. A walk-in and an
                                     appointment end through different screens,
                                     and anything driving this panel has to be
                                     able to tell them apart before it presses. -->
                                <div t-foreach="g.customers" t-as="c" t-key="c.key"
                                     t-att-data-kind="c.kind"
                                     class="modryn_team_client">
                                    <div class="modryn_card_top">
                                        <div>
                                            <div class="modryn_strong"><t t-esc="c.name"/></div>
                                            <div t-if="c.detail" class="modryn_sub" dir="ltr">
                                                <t t-esc="c.detail"/>
                                            </div>
                                        </div>
                                        <span t-if="c.kind === 'queue'"
                                              class="modryn_badge is_muted ms-auto">Walk-in</span>
                                        <span t-else="" class="modryn_badge is_muted ms-auto">Booking</span>
                                    </div>
                                    <div class="modryn_card_marks">
                                        <span t-if="c.clientType === 'bride'"
                                              class="modryn_badge is_free">Bride</span>
                                        <span t-if="c.room" class="modryn_badge is_muted"
                                              t-esc="c.room"/>
                                    </div>
                                    <!-- What the stylist wrote for the team. The
                                         one field on this board meant to be read
                                         by whoever picks her up next. -->
                                    <div t-if="c.note" class="modryn_sub" t-esc="c.note"/>
                                    <div t-if="c.helpers.length" class="modryn_sub">
                                        <t t-foreach="c.helpers" t-as="h" t-key="h.id">
                                            <t t-esc="h.name"/><t t-if="!h_last">, </t>
                                        </t>
                                    </div>
                                    <div class="modryn_card_actions">
                                        <!-- Back to the line is a walk-in's move
                                             only: a booking has no line to go
                                             back to, and the route refuses it. -->
                                        <button t-if="c.kind === 'queue' and mayClose(c)"
                                                class="btn btn-sm btn-outline-dark modryn_team_back"
                                                t-on-click="() => this.release('queue', c.id)">
                                            Back to the line
                                        </button>
                                        <button t-if="mayClose(c)"
                                                class="btn btn-sm btn-primary modryn_team_finish"
                                                t-on-click="() => this.endVisit(c)">
                                            Finished
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- ============ today's appointments ============ -->
                    <section class="modryn_panel modryn_bookings_panel">
                        <header class="modryn_panel_head">
                            <h2>Today's appointments</h2>
                            <span class="modryn_count"><t t-esc="state.bookings.length"/></span>
                            <!-- The nag (modryn_ops): conversion numbers are made of
                                 outcomes, so past bookings without one must not be
                                 allowed to quietly evaporate. The board only renders
                                 today, so the badge opens the full list to act on. -->
                            <button t-if="state.unclosed" class="modryn_badge is_busy btn btn-sm p-0 border-0"
                                    t-on-click="openUnclosed">
                                <t t-esc="state.unclosed"/> to close
                            </button>
                        </header>

                        <p t-if="!state.bookings.length" class="modryn_empty">No appointments today.</p>

                        <div class="modryn_cards">
                            <div t-foreach="state.bookings" t-as="b" t-key="b.id"
                                 class="modryn_customer_card"
                                 data-drop-target="booking" t-att-data-drop-id="b.id">
                                <div class="modryn_card_top">
                                    <span class="modryn_pos"><t t-esc="b.time"/></span>
                                    <div>
                                        <div class="modryn_strong"><t t-esc="b.title"/></div>
                                        <div class="modryn_sub" dir="ltr"><t t-esc="b.phone"/></div>
                                        <div t-if="b.dress" class="modryn_sub">
                                            <t t-esc="b.dress"/><t t-if="b.size"> · <t t-esc="b.size"/></t>
                                        </div>
                                    </div>
                                    <!-- Closed bookings wear their outcome; a manager may
                                         tap it to correct a mistake (force, server-checked). -->
                                    <t t-if="b.outcome">
                                        <button t-if="state.canAssign"
                                                t-attf-class="modryn_badge ms-auto btn btn-sm p-0 border-0 #{b.outcome === 'sold' ? 'is_free' : 'is_muted'}"
                                                t-on-click="() => this.openOutcome(b)">
                                            <t t-esc="outcomeLabel(b.outcome)"/>
                                        </button>
                                        <span t-else=""
                                              t-attf-class="modryn_badge ms-auto #{b.outcome === 'sold' ? 'is_free' : 'is_muted'}">
                                            <t t-esc="outcomeLabel(b.outcome)"/>
                                        </span>
                                    </t>
                                </div>

                                <t t-call="modryn_staff.CardPeople">
                                    <t t-set="card" t-value="b"/>
                                    <t t-set="kind" t-value="'booking'"/>
                                </t>

                                <div class="modryn_card_actions">
                                    <select class="form-select form-select-sm w-auto"
                                            t-att-value="b.room_id or ''"
                                            t-on-change="(ev) => this.setRoom('booking', b.id, ev)">
                                        <option value="">— No room —</option>
                                        <t t-foreach="state.rooms" t-as="r" t-key="r.id">
                                            <option t-att-value="r.id"
                                                    t-att-selected="r.id === b.room_id"><t t-esc="r.name"/></option>
                                        </t>
                                    </select>
                                    <button t-if="b.phone" class="btn btn-sm btn-outline-dark"
                                            t-on-click="() => this.openCustomer(b.title, b.phone)">
                                        Profile
                                    </button>
                                    <button class="btn btn-sm btn-outline-danger"
                                            t-on-click="() => this.openSos('booking', b.id)">
                                        Call for help
                                    </button>
                                </div>
                                <div t-if="state.canAssign or (!b.outcome and canClose(b))" class="modryn_card_actions">
                                    <select t-if="state.canAssign" class="form-select form-select-sm w-auto"
                                            t-on-change="(ev) => this.assignFromSelect('booking', b.id, ev)">
                                        <option value="">— Assign —</option>
                                        <t t-foreach="state.staff" t-as="s" t-key="s.id">
                                            <option t-att-value="s.id"><t t-esc="s.name"/></option>
                                        </t>
                                    </select>
                                    <!-- Her own stylist may close the fitting; the server
                                         re-checks, this only decides what to draw. -->
                                    <button t-if="!b.outcome and canClose(b)"
                                            class="btn btn-sm btn-primary"
                                            t-on-click="() => this.openOutcome(b)">Done</button>
                                </div>
                            </div>
                        </div>
                    </section>

                    <!-- ============ today's checklist + follow-up work (modryn_ops) ============ -->
                    <section t-if="state.checklist.length or state.opsTasks.length" class="modryn_panel">
                        <header class="modryn_panel_head">
                            <h2>Today's tasks</h2>
                            <span class="modryn_count">
                                <t t-esc="state.checklist.filter((t) => t.state === 'open').length + state.opsTasks.length"/> open
                            </span>
                        </header>

                        <table t-if="state.checklist.length" class="table modryn_table align-middle">
                            <tbody>
                                <tr t-foreach="state.checklist" t-as="task" t-key="task.id">
                                    <td class="w-auto">
                                        <input type="checkbox" class="form-check-input"
                                               t-att-checked="task.state === 'done'"
                                               t-att-disabled="task.state === 'done' and !state.canAssign"
                                               t-on-click="() => task.state === 'open' ? this.doneOpsTask(task.id) : (state.canAssign ? this.reopenOpsTask(task.id) : null)"/>
                                    </td>
                                    <td>
                                        <span t-attf-class="#{task.state === 'done' ? 'text-decoration-line-through modryn_sub' : 'modryn_strong'}">
                                            <t t-esc="task.name"/>
                                        </span>
                                        <span t-if="task.done_by" class="modryn_sub"> · <t t-esc="task.done_by"/></span>
                                    </td>
                                    <td class="text-end">
                                        <span t-attf-class="modryn_badge #{task.overdue ? 'is_busy' : 'is_muted'}">
                                            <t t-esc="task.due"/>
                                        </span>
                                    </td>
                                </tr>
                            </tbody>
                        </table>

                        <table t-if="state.opsTasks.length" class="table modryn_table align-middle">
                            <tbody>
                                <tr t-foreach="state.opsTasks" t-as="task" t-key="task.id">
                                    <td>
                                        <div class="modryn_strong"><t t-esc="task.name"/></div>
                                        <div t-if="task.phone" class="modryn_sub" dir="ltr"><t t-esc="task.phone"/></div>
                                        <div t-if="task.note" class="modryn_sub"><t t-esc="task.note"/></div>
                                    </td>
                                    <td>
                                        <span t-if="task.assignee" class="modryn_badge is_muted"><t t-esc="task.assignee"/></span>
                                    </td>
                                    <td>
                                        <span dir="ltr" t-attf-class="modryn_badge #{task.overdue ? 'is_busy' : 'is_muted'}">
                                            <t t-esc="task.due_date"/> <t t-esc="task.due"/>
                                        </span>
                                    </td>
                                    <td class="text-end">
                                        <button class="btn btn-sm btn-primary"
                                                t-on-click="() => this.doneOpsTask(task.id)">Done</button>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </section>

                    <!-- ============ my alterations (seamstress self-view) ============ -->
                    <section t-if="state.myTasks.length" class="modryn_panel">
                        <header class="modryn_panel_head">
                            <h2>My alterations</h2>
                            <span class="modryn_count"><t t-esc="state.myTasks.length"/></span>
                        </header>
                        <table class="table modryn_table align-middle">
                            <tbody>
                                <tr t-foreach="state.myTasks" t-as="task" t-key="task.id">
                                    <td>
                                        <span t-if="task.priority === '2'" class="modryn_badge is_busy me-1">High</span>
                                        <span t-elif="task.priority === '0'" class="modryn_badge is_muted me-1">Low</span>
                                        <div class="modryn_strong"><t t-esc="task.customer"/></div>
                                        <div t-if="task.dress" class="modryn_sub">
                                            <t t-esc="task.dress"/><t t-if="task.size"> · <t t-esc="task.size"/></t>
                                        </div>
                                    </td>
                                    <td>
                                        <span t-foreach="task.pieces" t-as="p" t-key="p"
                                              class="modryn_badge is_muted me-1"><t t-esc="p"/></span>
                                        <div t-if="task.note" class="modryn_sub"><t t-esc="task.note"/></div>
                                    </td>
                                    <td>
                                        <span t-attf-class="modryn_badge #{task.overdue ? 'is_busy' : 'is_muted'}">
                                            <t t-esc="task.due"/>
                                        </span>
                                    </td>
                                    <td class="text-end">
                                        <button t-if="task.state === 'intake'"
                                                class="btn btn-sm btn-primary"
                                                t-on-click="() => this.advanceTask(task.id, 'in_progress')">
                                            Start
                                        </button>
                                        <button t-if="task.state === 'in_progress'"
                                                class="btn btn-sm btn-primary"
                                                t-on-click="() => this.advanceTask(task.id, 'ready')">
                                            Ready
                                        </button>
                                        <span t-if="task.state === 'ready'" class="modryn_badge is_free">
                                            Ready
                                        </span>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                    <!-- ============ anybody who has ever been in ============ -->
                    <!-- Last, because every panel above it is about NOW and this
                         one is the only way to reach a visit that is over. The
                         moment a customer is finished she leaves every other
                         list on this screen, so "the bride who came on Tuesday"
                         had no answer anywhere in the product. -->
                    <section class="modryn_panel modryn_lookup_panel">
                        <header class="modryn_panel_head">
                            <h2>Find a customer</h2>
                        </header>
                        <p class="modryn_sub">
                            By name, phone or ticket number — including everyone
                            who has already left.
                        </p>
                        <div class="d-flex gap-2 flex-wrap align-items-center">
                            <input type="search" class="form-control w-auto"
                                   t-model="state.lookup"
                                   t-on-keyup="onLookupKey"
                                   placeholder="Name, phone, or number"/>
                            <button class="btn btn-sm btn-outline-dark modryn_lookup_go"
                                    t-on-click="lookupCustomers">Look</button>
                            <button t-if="state.lookupRows" class="btn btn-sm btn-link"
                                    t-on-click="clearLookup">Clear</button>
                            <span t-if="state.lookupBusy" class="modryn_sub">…</span>
                        </div>

                        <p t-if="state.lookupRows and !state.lookupRows.length"
                           class="modryn_empty mt-3">Nobody by that.</p>
                        <table t-if="state.lookupRows and state.lookupRows.length"
                               class="table modryn_table align-middle mt-3">
                            <tbody>
                                <tr t-foreach="state.lookupRows" t-as="row"
                                    t-key="row.kind + row.id">
                                    <td>
                                        <div class="modryn_strong"><t t-esc="row.name"/></div>
                                        <div t-if="row.note" class="modryn_sub"><t t-esc="row.note"/></div>
                                    </td>
                                    <td class="modryn_sub" dir="ltr"><t t-esc="row.phone"/></td>
                                    <td class="modryn_sub" dir="ltr"><t t-esc="row.id"/></td>
                                    <td class="modryn_sub" dir="ltr"><t t-esc="row.when"/></td>
                                    <td class="modryn_sub"><t t-esc="row.who"/></td>
                                    <td>
                                        <span t-if="row.kind === 'booking'"
                                              class="modryn_badge is_muted">Booking</span>
                                        <span t-else="" class="modryn_badge is_muted">Walk-in</span>
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </section>
                </div>

                <!-- ============ the bench: the whole team, live ============ -->
                <aside class="modryn_bench" data-drop-target="bench">
                    <!-- A hand up, from anywhere. Calling for help was reachable
                         only from a CUSTOMER's card, so a woman who needs
                         somebody for a hem, a delivery or the till had to pick
                         a bride's card to ask through. This one belongs to no
                         customer, which is most of the reasons to ask. -->
                    <button t-if="state.canTake" class="btn btn-danger w-100 mb-3 modryn_sos_open"
                            t-on-click="() => this.openSos(null, null)">
                        <i class="fa fa-bell" aria-hidden="true"/>
                        I need a hand
                    </button>

                    <header class="modryn_panel_head">
                        <!-- Kept as "Team on shift" deliberately. The wording is
                             loose — this is every active staff member, so that
                             somebody covering for a sick colleague stays
                             draggable — and modryn_roster marks who is really on
                             the rota row by row below. Renaming it to "Team"
                             orphaned its Hebrew: the existing msgid "Team" in
                             he.po is a server-view term, and only an entry
                             carrying an `odoo-javascript` comment reaches the
                             web catalogue this template reads. A truer English
                             header is not worth an untranslated Hebrew one. -->
                        <h2>Team on shift</h2>
                        <span class="modryn_count"><t t-esc="freeCount"/> available</span>
                    </header>
                    <p t-if="!state.staff.length" class="modryn_empty">No team members have been set up yet.</p>
                    <div class="modryn_bench_list">
                        <div t-foreach="state.staff" t-as="s" t-key="s.id"
                             t-attf-class="modryn_bench_row #{s.occupied ? 'is_busy' : 'is_free'}">
                            <span class="modryn_chip" t-att-data-employee="s.id">
                                <t t-esc="s.name"/>
                            </span>
                            <div class="modryn_bench_meta">
                                <span class="modryn_sub" t-esc="s.role"/>
                                <!-- Text, not colour alone: status must survive a
                                     colour-blind reader and a greyscale screen. -->
                                <span class="modryn_status">
                                    <t t-if="s.occupied">Busy</t>
                                    <t t-else="">Available</t>
                                </span>
                                <span t-if="s.occupied and s.occupied_with" class="modryn_sub">
                                    · <t t-esc="s.occupied_with"/>
                                </span>
                                <!-- The sentence arrives already translated from
                                     modryn_roster, because a literal here would
                                     need a POT re-export to reach Hebrew and the
                                     staff terminal defaults to he_IL. Empty (or
                                     undefined, when modryn_roster is not
                                     installed) renders nothing. -->
                                <span t-if="s.off_rota_label" class="modryn_sub">
                                    · <t t-esc="s.off_rota_label"/>
                                </span>
                            </div>
                        </div>
                    </div>
                    <p t-if="state.canAssign" class="modryn_sub mt-2 mb-0">
                        Drag a name onto a customer to assign her.
                    </p>

                    <!-- My month (modryn_ops): my own numbers, nobody else's.
                         Private by decision — there is no leaderboard. -->
                    <div t-if="state.myStats" class="mt-4">
                        <header class="modryn_panel_head">
                            <h2>My month</h2>
                        </header>
                        <div class="modryn_bench_list">
                            <div class="modryn_bench_row">
                                <div class="modryn_bench_meta">
                                    <span class="modryn_sub">Fittings closed</span>
                                    <span class="modryn_strong"><t t-esc="state.myStats.closed"/></span>
                                </div>
                            </div>
                            <div class="modryn_bench_row">
                                <div class="modryn_bench_meta">
                                    <span class="modryn_sub">Sold</span>
                                    <span class="modryn_strong">
                                        <t t-esc="state.myStats.sold"/>
                                        (<t t-esc="state.myStats.conversion"/>%)
                                    </span>
                                </div>
                            </div>
                            <div class="modryn_bench_row">
                                <div class="modryn_bench_meta">
                                    <span class="modryn_sub">Follow-ups open</span>
                                    <span class="modryn_strong"><t t-esc="state.myStats.open_followups"/></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </aside>
            </div>

            <!-- ============ someone needs help, right now ============
                 Full-screen and unmissable on purpose. A toast in the corner is
                 exactly what a woman holding a dress with both hands misses. -->
            <div t-if="incomingSos.length" class="modryn_sos_overlay" role="alert">
                <div class="modryn_sos_card">
                    <t t-set="c" t-value="incomingSos[0]"/>
                    <p class="modryn_sos_kicker">
                        <t t-if="c.escalated">No one answered</t>
                        <t t-else="">Someone needs help</t>
                    </p>
                    <h2 class="modryn_sos_who"><t t-esc="c.caller_name"/></h2>
                    <p t-if="c.where" class="modryn_sos_where"><t t-esc="c.where"/></p>
                    <p t-if="c.note" class="modryn_sos_note"><t t-esc="c.note"/></p>
                    <!-- Classed, so anything driving this overlay finds the
                         button rather than the word. The words are translated
                         three ways and one of them - "تمت المعالجة" - is not
                         even a prefix of the others, which is how a helper
                         written against the labels quietly stopped clearing
                         anything in Arabic. -->
                    <div class="modryn_sos_actions">
                        <button t-if="c.state === 'open'"
                                class="btn btn-primary btn-lg modryn_sos_ack"
                                t-on-click="() => this.ackSos(c.id)">On my way</button>
                        <button t-else="" class="btn btn-primary btn-lg" disabled="disabled">
                            On the way
                        </button>
                        <button class="btn btn-outline-dark btn-lg modryn_sos_done"
                                t-on-click="() => this.resolveSos(c.id)">Handled</button>
                    </div>
                    <p t-if="incomingSos.length > 1" class="modryn_sub mt-3">
                        <t t-esc="incomingSos.length - 1"/> more waiting.
                    </p>
                </div>
            </div>

            <!-- ============ compose a call for help ============ -->
            <div t-if="state.sosForm" class="modryn_modal_backdrop" t-on-click.self="closeSos">
                <div class="modryn_modal" role="dialog" aria-modal="true">
                    <h2 class="mb-3">Who do you need?</h2>
                    <div class="mb-3">
                        <label class="form-label">Who</label>
                        <!-- Only the women who are HERE. A colleague who went
                             home an hour ago is a call nobody answers, and from
                             a list of names the caller cannot tell which is
                             which. "Any manager" stays first because it is the
                             right answer when she does not mind who comes. -->
                        <select class="form-select" t-model="state.sosForm.target_id">
                            <option value="">Any manager</option>
                            <t t-foreach="colleagues" t-as="s" t-key="s.id">
                                <option t-att-value="s.id">
                                    <t t-esc="s.name"/><t t-if="s.occupied"> (busy)</t>
                                </option>
                            </t>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">What for</label>
                        <input type="text" class="form-control" t-model="state.sosForm.note"
                               placeholder="A hand with a zip, a size from the back…"/>
                        <div class="modryn_sub">
                            She reads this before she walks over.
                        </div>
                    </div>
                    <div class="d-flex gap-2">
                        <button class="btn btn-danger" t-on-click="sendSos">Call for help</button>
                        <button class="btn btn-outline-dark" t-on-click="closeSos">Cancel</button>
                    </div>
                </div>
            </div>

            <!-- ============ unclosed past bookings (modryn_ops) ============
                 The rows behind the nag badge. Each opens the same outcome
                 modal a live card uses — that is how the count reaches zero. -->
            <div t-if="state.unclosedList" class="modryn_modal_backdrop" t-on-click.self="closeUnclosed">
                <div class="modryn_modal" role="dialog" aria-modal="true">
                    <h2 class="mb-1">Still to close</h2>
                    <p class="modryn_sub mb-3">
                        Past fittings without an outcome. Every one of them is missing
                        from the numbers until it's closed.
                    </p>
                    <p t-if="!state.unclosedList.length" class="modryn_empty">All closed. Well done.</p>
                    <table t-if="state.unclosedList.length" class="table modryn_table align-middle">
                        <tbody>
                            <tr t-foreach="state.unclosedList" t-as="u" t-key="u.id">
                                <td class="modryn_sub" dir="ltr"><t t-esc="u.when"/></td>
                                <td>
                                    <div class="modryn_strong"><t t-esc="u.title"/></div>
                                    <div t-if="u.phone" class="modryn_sub" dir="ltr"><t t-esc="u.phone"/></div>
                                </td>
                                <td class="text-end">
                                    <button class="btn btn-sm btn-primary"
                                            t-on-click="() => this.openOutcome(u)">Close</button>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                    <div class="d-flex gap-2">
                        <button class="btn btn-outline-dark" t-on-click="closeUnclosed">Done</button>
                    </div>
                </div>
            </div>

            <!-- ============ customer profile (modryn_ops) ============
                 The relationship data every card can open. Budget renders only
                 when the server sent it — its absence from the staff payload
                 IS the permission, mirrored here, enforced there. -->
            <div t-if="state.customer" class="modryn_modal_backdrop" t-on-click.self="closeCustomer">
                <div class="modryn_modal" role="dialog" aria-modal="true">
                    <h2 class="mb-1"><t t-esc="state.customer.name"/></h2>
                    <p class="modryn_sub mb-3" dir="ltr"><t t-esc="state.customer.phone"/></p>
                    <p t-if="state.customer.category" class="mb-3">
                        <span t-attf-class="modryn_badge #{state.customer.category === 'purchased' ? 'is_free' : 'is_muted'}">
                            <t t-if="state.customer.category === 'purchased'">Purchased</t>
                            <t t-else="">Not purchased yet</t>
                        </span>
                    </p>
                    <p t-if="state.customer.isNew" class="modryn_sub mb-3">
                        No profile yet — saving starts one.
                    </p>
                    <div t-if="state.modalError" class="modryn_field_error mb-3" role="alert">
                        <t t-esc="state.modalError"/>
                    </div>

                    <div class="modryn_modal_row mb-3">
                        <div>
                            <label class="form-label">Wedding date</label>
                            <input type="date" class="form-control" t-model="state.customer.form.wedding_date"/>
                        </div>
                        <div t-if="state.customer.hasBudget or (state.customer.isNew and state.canAssign)">
                            <label class="form-label">Budget (₪)</label>
                            <input type="number" min="0" step="100" class="form-control"
                                   t-model="state.customer.form.budget"/>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Party</label>
                        <input type="text" class="form-control"
                               placeholder="Mother + two bridesmaids…"
                               t-model="state.customer.form.party"/>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Measurements</label>
                        <textarea class="form-control" rows="2" t-model="state.customer.form.measurements"/>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Notes</label>
                        <textarea class="form-control" rows="2" t-model="state.customer.form.notes"/>
                    </div>

                    <div class="d-flex gap-2">
                        <button class="btn btn-primary" t-on-click="saveCustomer">Save</button>
                        <button class="btn btn-outline-dark" t-on-click="closeCustomer">Cancel</button>
                    </div>
                </div>
            </div>

            <!-- ============ outcome modal: how did the fitting end? ============
                 Served by modryn_ops. Three endings; each fires its flow on the
                 server (thank-you / feedback / rebook text). Choosing Sold chains
                 into the alteration modal below via the `finished` payload. -->
            <div t-if="state.outcomeForm" class="modryn_modal_backdrop" t-on-click.self="closeOutcome">
                <div class="modryn_modal" role="dialog" aria-modal="true">
                    <h2 class="mb-1">How did it end for <t t-esc="state.outcomeForm.customer"/>?</h2>
                    <p t-if="state.outcomeForm.existing" class="modryn_sub mb-3">
                        Currently recorded: <t t-esc="outcomeLabel(state.outcomeForm.existing)"/>.
                        Saving will replace it.
                    </p>
                    <div t-if="state.modalError" class="modryn_field_error mb-3" role="alert">
                        <t t-esc="state.modalError"/>
                    </div>

                    <div class="d-flex gap-2 mb-3">
                        <button t-attf-class="btn #{state.outcomeForm.kind === 'sold' ? 'btn-primary' : 'btn-outline-dark'}"
                                t-on-click="() => this.pickOutcome('sold')">Sold</button>
                        <button t-attf-class="btn #{state.outcomeForm.kind === 'not_sold' ? 'btn-primary' : 'btn-outline-dark'}"
                                t-on-click="() => this.pickOutcome('not_sold')">Not sold</button>
                        <button t-attf-class="btn #{state.outcomeForm.kind === 'no_show' ? 'btn-primary' : 'btn-outline-dark'}"
                                t-on-click="() => this.pickOutcome('no_show')">No-show</button>
                    </div>

                    <t t-if="state.outcomeForm.kind === 'sold'">
                        <div class="mb-3">
                            <label class="form-label">Amount (₪)</label>
                            <input type="number" min="0" step="1" class="form-control"
                                   t-model="state.outcomeForm.amount"/>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">What she bought</label>
                            <textarea class="form-control" rows="2"
                                      placeholder="Gown, veil, alterations…"
                                      t-model="state.outcomeForm.items"/>
                        </div>
                    </t>

                    <t t-if="state.outcomeForm.kind === 'not_sold'">
                        <div class="mb-3">
                            <label class="form-label">Note (optional)</label>
                            <input type="text" class="form-control"
                                   placeholder="Loved the second gown, thinking it over…"
                                   t-model="state.outcomeForm.note"/>
                        </div>
                        <p class="modryn_sub mb-3">She'll receive a feedback text.</p>
                    </t>

                    <t t-if="state.outcomeForm.kind === 'no_show'">
                        <p class="modryn_sub mb-3">She'll receive a warm text with a link to rebook.</p>
                    </t>

                    <div class="d-flex gap-2">
                        <button class="btn btn-primary" t-att-disabled="!state.outcomeForm.kind"
                                t-on-click="saveOutcome">Save</button>
                        <button class="btn btn-outline-dark" t-on-click="closeOutcome">Cancel</button>
                    </div>
                </div>
            </div>

            <!-- ============ finish modal: fitting done -> alteration task ============ -->
            <div t-if="state.finish" class="modryn_modal_backdrop" t-on-click.self="closeFinish">
                <div class="modryn_modal" role="dialog" aria-modal="true">
                    <h2 class="mb-1">How did it end for <t t-esc="state.finish.customer"/>?</h2>

                    <!-- Already answered. The question collapses into a line
                         rather than staying open: a second tap on "she took
                         this one" is a second dress off the rail. -->
                    <div t-if="state.finish.outcome" class="modryn_outcome_done mb-4">
                        <t t-if="state.finish.outcome === 'sold'">
                            <span class="modryn_badge is_free">Sold</span>
                            <span class="modryn_strong" t-esc="state.finish.dressLabel"/>
                            <div class="modryn_sub">Taken off the rail.</div>
                        </t>
                        <t t-else="">
                            <span class="modryn_badge is_muted">Left without buying</span>
                            <div class="modryn_sub">Nothing came off the rail.</div>
                        </t>
                    </div>

                    <t t-else="">
                    <div class="mb-3">
                        <label class="form-label">Dress</label>
                        <!-- Chosen already: her answer, and a way to change it.
                             No list, no box - the question is settled. -->
                        <div t-if="state.finish.form.variant_id" class="modryn_dress_picked">
                            <span class="modryn_strong" t-esc="state.finish.dressLabel"/>
                            <button type="button" class="btn btn-sm btn-link"
                                    t-on-click="clearDress">Change</button>
                        </div>
                        <t t-else="">
                            <input type="search" class="form-control"
                                   t-model="state.finish.dressQuery"
                                   placeholder="Name, serial number, or kind"/>
                            <div class="modryn_sub">From two letters onwards.</div>
                            <div t-if="dressMatches.length" class="modryn_dress_hits">
                                <button t-foreach="dressMatches" t-as="v" t-key="v.id"
                                        type="button" class="modryn_dress_hit"
                                        t-att-data-variant="v.id"
                                        t-on-click="() => this.pickDress(v)">
                                    <span t-esc="v.label"/>
                                    <span t-if="v.serial" class="modryn_sub" dir="ltr"
                                          t-esc="v.serial"/>
                                    <!-- What is left of that size. A stylist
                                         about to write down the last one should
                                         be able to see that it is the last one.
                                         No "none left" branch: the server sends
                                         only sizes that exist, so a nought here
                                         could never render and a branch that
                                         cannot render is a lie about what this
                                         screen can say. -->
                                    <span class="modryn_badge is_free" t-esc="v.stock"/>
                                </button>
                            </div>
                            <div t-elif="state.finish.dressQuery.trim().length >= 2"
                                 class="modryn_sub">Nothing matches that.</div>
                        </t>
                    </div>

                    <!-- Waiting on a yes. The sentence names the gown and the
                         number, because "are you sure?" over a thousand
                         dresses tells her nothing about which one. -->
                    <div t-if="state.finish.confirm" class="modryn_finish_confirm mb-4" role="alert">
                        <p class="mb-2"><t t-esc="confirmSentence"/></p>
                        <div class="d-flex gap-2">
                            <button class="btn btn-sm btn-primary"
                                    t-on-click="confirmOutcome">Yes</button>
                            <button class="btn btn-sm btn-link"
                                    t-on-click="cancelOutcome">No, go back</button>
                        </div>
                    </div>
                    <div t-else="" class="d-flex flex-wrap gap-2 mb-4">
                        <button class="btn btn-primary modryn_outcome_sold"
                                t-on-click="() => this.askOutcome('sold')">She took this one</button>
                        <button class="btn btn-outline-dark modryn_outcome_not_sold"
                                t-on-click="() => this.askOutcome('not_sold')">She left without buying</button>
                    </div>
                    </t>

                    <!-- The workshop, and ONLY once there is something to send
                         it. A customer who bought nothing has nothing to alter;
                         a bride whose gown fits has nothing to alter either, and
                         a required date sitting under her name says otherwise.
                         So it is a question she opens, not a form she closes. -->
                    <t t-if="state.finish.outcome === 'sold' and !state.finish.workshop">
                        <hr class="my-4"/>
                        <button class="btn btn-outline-dark modryn_open_workshop"
                                t-on-click="openWorkshop">The gown needs altering</button>
                    </t>

                    <t t-if="state.finish.workshop">
                    <hr class="my-4"/>
                    <p class="modryn_sub mb-3">
                        What the workshop needs to know.
                    </p>

                    <div class="mb-3">
                        <label class="form-label">Pieces to alter</label>
                        <div class="modryn_piece_grid">
                            <label t-foreach="state.atelier.pieces" t-as="p" t-key="p.id"
                                   class="modryn_piece_check">
                                <input type="checkbox"
                                       t-on-change="(ev) => this.togglePiece(p.id, ev)"/>
                                <span t-esc="p.name"/>
                            </label>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Instructions</label>
                        <textarea class="form-control" rows="2" t-model="state.finish.form.note"/>
                    </div>

                    <div class="modryn_modal_row mb-4">
                        <div>
                            <label class="form-label">Seamstress</label>
                            <select class="form-select" t-model="state.finish.form.seamstress_id">
                                <option value="">— Later —</option>
                                <t t-foreach="seamstresses" t-as="s" t-key="s.id">
                                    <option t-att-value="s.id"><t t-esc="s.name"/></option>
                                </t>
                            </select>
                        </div>
                        <div>
                            <label class="form-label">Priority</label>
                            <select class="form-select" t-model="state.finish.form.priority">
                                <option value="2">High</option>
                                <option value="1">Normal</option>
                                <option value="0">Low</option>
                            </select>
                        </div>
                        <div>
                            <label class="form-label">Due</label>
                            <input type="date" class="form-control" required="required"
                                   t-model="state.finish.form.due_date"/>
                        </div>
                    </div>

                    </t>

                    <div t-if="state.finish.error" class="alert alert-danger" role="alert">
                        <t t-esc="state.finish.error"/>
                    </div>

                    <div class="d-flex gap-2 mt-3">
                        <button t-if="state.finish.workshop" class="btn btn-primary"
                                t-on-click="createTask">Send to the workshop</button>
                        <!-- Not "Skip" any more: by the time she reaches this
                             row the ending is recorded, and calling that "skip"
                             reads as throwing it away. -->
                        <button class="btn btn-link modryn_finish_close" t-on-click="closeFinish">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </t>

</templates>
